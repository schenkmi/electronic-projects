# Releases

The files in this directory are created by the [github release workflow](.github/workflows/release.yml).

They are used by the One ROM USB Programmer to discover the firmware image release artifacts, so they can be automatically downloaded and programmed. 
