# Blank config file
#
# Included by the Makefile if no other config is specified.
#
# Rather than modify this config file, create a new file in the same directory and include it like this:
#
# CONFIG=configs/your_config.mk make
